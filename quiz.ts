const allTopics: ["complex_polynomials", "vector_projections", "chemical_equilibria"]
const allQuestions: {
  [topic: string]: {
	  question: string
	  solution: string
  }[]
} = {
  complex_polynomials: [{
    question: "Solve z^2 + 1 = 0",
    solution: "±i"
  }]
}

const generate = () => {

}
